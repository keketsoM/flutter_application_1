# Precusor: Setup git 
git init
 // Setup your git credentials
# 1. Commit changes to Git
 git add <file/files>
 git commit -m "Added chat feature"

 # 2. Undo a commit
 git reset --soft HEAD^

 # 3. Restore a file from previous commit
 git restore -p <commit hash> file


 # 5 Merge branch back to main branch
 git merge mainBranch // (you do it whilst inside newBranch)

